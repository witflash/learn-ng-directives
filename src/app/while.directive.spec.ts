import { WhileDirective } from './while.directive';

describe('WhileDirective', () => {
  it('should create an instance', () => {
    const directive = new WhileDirective();
    expect(directive).toBeTruthy();
  });
});
